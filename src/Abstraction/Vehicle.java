package Abstraction;
//this is an abstract class, because we always want a type of Vehicle, but never only a vehicle.
public abstract class Vehicle {

	abstract void go();			//this has not a body, also not { } 
}
